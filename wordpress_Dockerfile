FROM wordpress

ENV WORDPRESS_DB_HOST=mysql_container   
#Give your mysql containers name

ENV WORDPRESS_DB_USER=root    
#Give db host name ,by default:root

ENV WORDPRESS_DB_PASSWORD=pass1234   
#same password as mysql container

ENV WORDPRESS_DB_NAME=wp_db   
#Give same name as given in mysql container 

EXPOSE 80
